# Local development environment - this file takes precedence over all other .env files
# This ensures we use the local backend for development
VITE_API_URL=http://localhost:3002

# Optional: Add other local development overrides here
NODE_ENV=development
