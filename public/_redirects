# Redirects for Single Page Application (SPA) routing
# This ensures all routes are handled by React Router

# Handle all routes - redirect everything to index.html for client-side routing
/*    /index.html   200

# API routes should go to the backend (if needed)
# /api/*  https://astrova-backend.onrender.com/api/:splat  200
